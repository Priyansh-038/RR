## Packages
framer-motion | Animations for UI transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
WebSocket connection required at location.protocol + '//' + location.host + '/ws'
Game rendering uses HTML5 Canvas
Retro pixel art aesthetic implemented via CSS and Canvas primitives
